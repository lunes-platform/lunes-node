package io.lunes.network.security

import org.scalatest.funsuite.AnyFunSuite

import io.lunes.state2.StateReader

/**
 * Test all transaction with (and not) banned address
 */
class SecurityCheckerSpec extends AnyFunSuite {

  test("first test") {
    assert(1 == 1)
  }
}
